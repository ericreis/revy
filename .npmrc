# revy depends only on public packages, so this repo always uses the public
# npm registry - independent of any machine-global registry (e.g. a corporate
# Artifactory). This keeps local dev and CI consistent and lets GitHub-hosted
# runners install dependencies.
registry=https://registry.npmjs.org/
